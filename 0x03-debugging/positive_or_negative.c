#include "main.h"

void positive_or_negativeoi(int i)
{  
       	int i;
        
        if (i == 0)
        {
                printf("%d is zero\n", i);
        }
        else if (i < 0)
        {
                printf("%d is negative\n", i);
        }

	else if (i > 0)
	{
		print("%d is positive\n", i);
	}
        
        return (0);
}

