#include "main.h"

/**
 * main -  checks for uppercase character.
 *
 * Return: Always 0.
 */
int main(void)
{
    char c;

    if (c>=65 && c<=90)
    {
	   
	    _isupper(c));
	    return (1);
    }
    else 
    {
	    return (0);
    }
    return 0;
}
