#include "main.h"
/**
 * void _print_rev_recursion(char *s) - prints string in reverse
 *
 *
 */
void _print_rev_recursion(char *s)
{
	int length = strlen(str);
	for(int i = lenth - 1; i >=0; i--)
	{
		printf("%c%", str[i]);
	}
}
