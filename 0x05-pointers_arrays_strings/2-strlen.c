#include "main.h"
#include <stdio.h>
/**
 * 2-strlen - returns the length of a string.
 * @s:string 
 * @len: length of string
 * Return; 0
 */
int _strlen(char *str)
{
	char *str;
	int len;
	
	str = "My first strlen!";
	len = _strlen(str);
       
	return (0);
}
