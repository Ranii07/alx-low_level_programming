#include "main.h"
#include <stdio.h>
/**
 * 2-strlen - returns the length of a string.
 * @s:string 
 * @len: length of string
 * Return; 0
 */
int _strlen(char *s)
{
	char s[] = "My first sting";
	int len = _strlen(s);
	return (0);
}
